/*Union type = Dizer a variável que ela pode guardar os tipos que foram
definidos na variável, no exemplo abaixo apenas numeros e strings. 
*/
let userId: number | string;

// userId = 192;
userId = "192";
console.log(typeof userId);
